/** Holding companies for techno report (shared DB defaults). */
export const TECHNO_DYUMANS_ID = Number(
  process.env.TECHNO_DYUMANS_ID ?? 1,
);
export const TECHNO_EWB_ID = Number(process.env.TECHNO_EWB_ID ?? 2);
export const TECHNO_KLIMANA_ID = Number(process.env.TECHNO_KLIMANA_ID ?? 3);
