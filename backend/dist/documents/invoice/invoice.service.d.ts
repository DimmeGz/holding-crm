import { DataSource, Repository } from 'typeorm';
import { CompaniesService } from '../../companies';
import { GoodsService } from '../../goods';
import { OrdersService } from '../orders';
import { PaymentService } from '../payment';
import { ShipmentService } from '../shipment';
import { WarehouseService } from '../../warehouse';
import { Invoice } from './entities';
import { CreateInvoiceByContractDTO, CreateInvoiceDTO, UpdateInvoiceDTO, UpdatePaymentBalanceDTO } from './dto';
import { GetInvoiceResponseDTO } from './dto/response-dto';
import { GetInvoicesQueryDTO } from './dto/query-dto';
export declare class InvoiceService {
    private readonly invoiceRepository;
    private dataSource;
    private readonly ordersService;
    private readonly paymentsService;
    private readonly companiesService;
    private readonly goodsService;
    private readonly shipmentsService;
    private readonly warehouseService;
    constructor(invoiceRepository: Repository<Invoice>, dataSource: DataSource, ordersService: OrdersService, paymentsService: PaymentService, companiesService: CompaniesService, goodsService: GoodsService, shipmentsService: ShipmentService, warehouseService: WarehouseService);
    private createBaseQueryBuilder;
    private applyInvoiceListSelect;
    private applyInvoiceDetailSelect;
    private applyQueryFilter;
    getInvoices(query?: GetInvoicesQueryDTO): Promise<Invoice[]>;
    getInvoiceById(invoiceId: number): Promise<GetInvoiceResponseDTO>;
    getInvoicesByOrderId(orderId: number): Promise<Invoice[]>;
    createInvoice(createInvoiceDTO: CreateInvoiceDTO): Promise<Invoice>;
    private calculateDocumentSum;
    private getTechnicalProcesses;
    private populateLinesCosts;
    createInvoiceByContract(createInvoiceByContractDTO: CreateInvoiceByContractDTO): Promise<Invoice>;
    updateInvoice(invoiceId: number, updateInvoiceDTO: UpdateInvoiceDTO): Promise<Invoice>;
    removeInvoice(invoiceId: number): Promise<Invoice>;
    changeInvoiceStatus(invoiceId: number): Promise<Invoice>;
    getInvoiceDataForCommission(invoiceId: number): Promise<Invoice>;
    private countInvoiceGrossWeight;
    changePaymentBalance(updateBalanceDTO: UpdatePaymentBalanceDTO): Promise<void>;
}
